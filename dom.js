// Example of using JavaScript to search for elements in an HTML document
let para = document.getElementById("early_languages");
let listItems = document.querySelectorAll("li");

console.log(para); 
console.log(listItems); // Elements will show in the browser console